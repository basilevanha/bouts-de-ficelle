{% macro sprite(id, width, height, label, path)  %}
	<span class="svg svg-{{ id }}">
		<svg width="{{ width }}" height="{{ height }}" {% if label %} aria-label="{{ label }}" {% endif %}>
			<use xlink:href="{{ path }}/static/sprite.svg#{{ id }}"></use>
		</svg>
	</span>
{% endmacro %}