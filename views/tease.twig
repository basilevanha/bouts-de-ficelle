<article class="tease tease-{{post.type}}" id="tease-{{post.id}}">
	{% block main %}
		<h2 class="h2"><a href="{{post.link}}">{{ title }}</a></h2>
		<p>{{ post.excerpt }}</p>
		{% if post.thumbnail %}
			<img src="{{post.thumbnail.src}}" />
		{% endif %}
	{% endblock %}
</article>