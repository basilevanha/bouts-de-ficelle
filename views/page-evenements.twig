{% extends "base.twig" %}

{% set template = 'evenements' %}

{# Variables disponibles :
		{{ dump(hero) }}
		{{ dump(liste) }}
 #}

{% block main %}

	<div class="container post-type-{{post.post_type}}" id="post-{{post.ID}}">

		BIENVENUE SUR LA PAGE EVENEMENTS
		
		<h1>HERO</h1>
		<p>{{ hero.titre }}</p>
		<p>{{ hero.texte }}</p>
		<img src="{{ hero.img.url }}" alt="{{ hero.img.alt }}">
		<p>{{ hero.btn.label }}</p>
		<p>{{ hero.btn.url }}</p>

		<h1>NOS ATELIERS</h1>
		<p>{{ liste.titre }}</p>
		
		<ul>
			{% for evenement in evenements %}
				<li>
					{{evenement.id}}
				</li>
			{% endfor %}
		</ul>
	</div>
	<!-- /content-wrapper -->
{% endblock %}
